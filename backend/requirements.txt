fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.1
google-genai==1.3.0
httpx>=0.27,<1  # Supabase Storage REST (rag/page_store.py)
pypdf==5.5.0
pymupdf>=1.26.5
sqlalchemy>=2.0,<3
psycopg[binary]>=3.2,<4
# Chỉ dùng cho đường tìm kiếm `memory` trong rag/vector_store.py (khi Postgres không bật
# được extension vector). Thiếu nó vẫn chạy đúng, chỉ chậm hơn ~180 lần mỗi câu hỏi.
numpy>=1.26,<3
PyJWT>=2.8,<3
edge-tts>=7.2,<8
