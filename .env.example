# Frontend (Vite). Copy thành ".env.local" nếu cần; trên Vercel/Netlify nhập vào mục Environment Variables.
# Để trống khi chạy local (Vite proxy /api sang FastAPI). Khi deploy: URL backend, ví dụ https://gia-su-ai-api.onrender.com
VITE_API_BASE_URL=
